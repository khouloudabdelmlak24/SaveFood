<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.text.Text?>
<?import javafx.scene.text.Font?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="tn.esprit.controllers.AfficherReponseController"
            style="-fx-background-color: linear-gradient(to bottom right, #eef2f7, #36656B);">

    <!-- ===================== HEADER ===================== -->
    <top>
        <VBox spacing="15" style="-fx-padding:25;">

            <!-- Titre -->
            <Label text="Gérer les  Réponses"
                   style="-fx-font-size:24px;
                          -fx-font-weight:bold;
                          -fx-text-fill:#36656B;" />

            <!-- Barre de recherche -->
            <HBox spacing="15" alignment="CENTER_LEFT">

                <TextField fx:id="searchField"
                           promptText="🔍 Rechercher par message..."
                           prefWidth="300"
                           style="-fx-background-radius:20;
                                  -fx-padding:8 15 8 15;
                                  -fx-border-radius:20;
                                  -fx-border-color:#3498db;
                                  -fx-focus-color:#3498db;" />

                <Button text="Rechercher"
                        onAction="#rechercher"
                        style="-fx-background-color:#3498db;
                               -fx-text-fill:white;
                               -fx-background-radius:20;
                               -fx-padding:8 20 8 20;
                               -fx-cursor:hand;" />

                <Button text="📅 Trier par Date"
                        onAction="#trierDate"
                        style="-fx-background-color:#2ecc71;
                               -fx-text-fill:white;
                               -fx-background-radius:20;
                               -fx-padding:8 20 8 20;
                               -fx-cursor:hand;" />

            </HBox>
        </VBox>
    </top>

    <!-- ===================== CONTENU ===================== -->
    <center>
        <ScrollPane fitToWidth="true"
                    style="-fx-background-color:transparent;">
            <VBox fx:id="cardContainer"
                  spacing="20"
                  style="-fx-padding:25;" />
        </ScrollPane>
    </center>

</BorderPane>
